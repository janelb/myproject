<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.libang.mapper.SchoolMapper">

    <select id="findById" parameterType="int" resultType="com.libang.entity.School">
            select *from school where id =#{id}

    </select>

</mapper>